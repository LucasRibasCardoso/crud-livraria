<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minha Livraria</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Minha Estante de Livros</h1>
            <a href="/livros/novo" class="btn-novo">Adicionar Novo Livro</a>
        </header>

        <main>
            <% if (livros.length > 0) { %>
                <div class="livros-grid">
                    <% livros.forEach(livro => { %>
                        <div class="card-livro">
                            <h3><%= livro.titulo %></h3>
                            <p class="autor">por <%= livro.autor %></p>
                            <p><strong>Editora:</strong> <%= livro.editora || 'N/A' %></p>
                            <p><strong>Ano:</strong> <%= livro.anoPublicacao || 'N/A' %></p>
                            <p><strong>ISBN:</strong> <%= livro.isbn || 'N/A' %></p>
                            <div class="card-actions">
                                <a href="/livros/editar/<%= livro.id %>" class="btn-editar">Editar</a>
                                <form action="/livros/excluir/<%= livro.id %>" method="POST" onsubmit="return confirm('Tem certeza que deseja excluir este livro?');">
                                    <button type="submit" class="btn-excluir">Excluir</button>
                                </form>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="sem-livros">Nenhum livro cadastrado ainda. Que tal adicionar um?</p>
            <% } %>
        </main>
    </div>
</body>
</html>